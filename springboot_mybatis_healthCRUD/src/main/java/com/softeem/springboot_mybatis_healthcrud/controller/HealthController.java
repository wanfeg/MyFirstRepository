package com.softeem.springboot_mybatis_healthcrud.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class HealthController {

//    @Autowired
//    private CheckItemService checkItemService;

/*    @RequestMapping(value = "/findAll")
    public List<CheckItem> student(Integer id) {
        List<CheckItem> checkItems = checkItemService.findAll();
        return checkItems;
    }

    @RequestMapping(value = "/findById")
    public CheckItem findById(Integer id) {
        CheckItem byId = checkItemService.findById(id);
        return byId;
    }

    @RequestMapping(value = "/findByName")
    public List<CheckItem> findByName(String name) {
        List<CheckItem> byName = checkItemService.findByName(name);
        return byName;
    }*/

}